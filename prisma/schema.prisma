generator client {
  provider = "prisma-client-js"
}

datasource at {
  provider  = "postgresql"
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}